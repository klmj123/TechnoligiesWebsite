import { PokemonAbilities } from '../models/pokemon-abilities';

describe('PokemonAbilities', () => {
  it('should create an instance', () => {
    expect(new PokemonAbilities()).toBeTruthy();
  });
});
